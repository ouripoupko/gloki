export class Community {
    constructor (tasks: any, members: any, nominates: any, properties: any) {
        console.log('tasks', tasks)
        console.log('members', members)
        console.log('nominates', nominates)
        console.log('properties', properties)
    }
}